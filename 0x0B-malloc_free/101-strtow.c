#include "main.h"
#include <stdlib.h>
/**
 * strtow - turn string into word
 * @str: the string
 * Return: pointer to the word
 */
char **strtow(char *str)
{
	char **ptr;

	str = "hello";	
	ptr = (int **)malloc(sizeof(int *));
	return(NULL);
}
